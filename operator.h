#ifndef OPERATOR_H
#define OPERATOR_H

#include <stdio.h>
#include <string.h>
#include "enum.h"
#include "lexical.h"

// Function Prototypes
status check_operator(char *, FILE *);
status single_operator_check(char);

#endif
